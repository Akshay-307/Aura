package com.aura.ui.base

/**
 * Interface implemented by fragments that wish to respond to bottom navigation
 * tab reselection (e.g. scrolling to top or resetting search state).
 */
interface OnTabReselectedListener {
    fun onTabReselected()
}

